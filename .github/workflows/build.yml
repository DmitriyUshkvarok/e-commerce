# name: Build
# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     types: [opened, synchronize, reopened]
# jobs:
#   sonarcloud:
#     name: SonarCloud
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#         with:
#           fetch-depth: 0 # Shallow clones should be disabled for a better relevancy of analysis
#       - name: SonarCloud Scan
#         uses: SonarSource/sonarcloud-github-action@master
#         env:
#           REMOVED: ${{ secrets.REMOVED }}
#           REMOVED: 7a0e897986e8021c8355e5db862c84e1ce726bda
#           SONAR_HOST_URL: https://sonarcloud.io

#       - name: SonarQube Quality Gate check
#         uses: sonarsource/sonarqube-quality-gate-action@master
#         timeout-minutes: 3
#         env:
#           REMOVED: 7a0e897986e8021c8355e5db862c84e1ce726bda
#           SONAR_HOST_URL: https://sonarcloud.io

name: Build and Deploy
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    steps:
      # Шаг клонирования репозитория
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Отключаем shallow clones для лучшего анализа

      # Запуск анализа SonarCloud
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          REMOVED: ${{ secrets.REMOVED }} # Для получения информации о PR
          REMOVED: ${{ secrets.REMOVED }}
          SONAR_HOST_URL: https://sonarcloud.io # URL SonarCloud

      # Проверка Quality Gate через SonarCloud API
      - name: Check SonarCloud Quality Gate
        run: |
          # Получаем статус Quality Gate через API
          QUALITY_GATE_STATUS=$(curl -s -u ${{ secrets.REMOVED }} "https://sonarcloud.io/api/qualitygates/project_status?projectKey=DmitriyUshkvarok_e-commerce" | jq -r .projectStatus.status)

          # Проверяем статус
          if [ "$QUALITY_GATE_STATUS" != "OK" ]; then
            echo "Quality Gate failed with status: $QUALITY_GATE_STATUS"
            exit 1 # Завершаем сборку с ошибкой, если статус не OK
          fi

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: sonarcloud
    steps:
      - name: Deploy
        run: |
          echo "Deploying to production..."
